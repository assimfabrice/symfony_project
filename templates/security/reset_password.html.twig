{% extends 'front/base.html.twig' %}

{% block body %}
    <h1>Nouveau mot de passe</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Réinitialiser</button>
    {{ form_end(form) }}
{% endblock %}